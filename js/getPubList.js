/**
 * Function only returns the list of pub ids. Include by adding getPubList = require('./getPubList')
 * @return {string[]}
 */

function getPubList() {
    let list =
        ["bulten", "japripps", "wijkanders", "cafec", "basen", "11an", "focus", "fortnox", "gasquen",
        "gastownospritkoket", "goldeni", "hubben21", "tagvagnen", "kajsabaren", "rodarummet",
        "verum", "winden", "zaloonen"];
    return list
}